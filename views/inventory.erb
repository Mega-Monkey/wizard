<h1>ITEMS</h1>

<% @items.each do |item| %>
<img src="<%= item.item_image %>" alt="">
<p><%= item.item_name %></p>

<form action="/market/<%= item.item_id %>" method="get">
    <button>Auction</button>
</form>
<% end %>