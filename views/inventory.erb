<h1>ITEMS</h1>

<% @items.each do |item| %>
<div class="item">
    <img src="<%= item.image %>" alt="">
    <p><%= item.name %></p>

    <form action="/market/<%= item.item_id %>" method="get">
        <button>Auction</button>
    </form>
    </div>
</div>
<% end %>